# --- Python ---
__pycache__/
*.pyc
*.pyo
.venv/
venv/
*.egg-info/

# --- מסדי נתונים מקומיים ---
*.db
*.sqlite
*.sqlite3

# --- קבצי סביבה (מכילים credentials אמיתיים) ---
.env
.env.*

# --- קבצי מיגרציה/relink - נוצרים בזמן הרצה, לא חלק מהקוד ---
document_relink_plan.csv
document_relink_backup_*.csv

# --- קבצים זמניים ---
*.tmp
*.log
.DS_Store
Thumbs.db

# --- קבצי העלאה מקומיים (תוכן משתמשים, לא קוד) ---
# הערה: מכיל מסמכי PDF אמיתיים שהמערכת עדיין קוראת מהם כ-fallback מקומי
# (ר' serve_upload). החרגה מ-Git אומרת שה-Git לא ישמש כגיבוי עבורם -
# המקור היחיד יהיה הדיסק בפועל (ו-Supabase למסמכים שהועברו). זו החלטה
# מכוונת: תוכן משתמשים לא שייך להיסטוריית קוד.
uploads/
